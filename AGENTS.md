# 🏛 Lumos 퀀트 트레이딩 시스템: 코드 수정 및 운영 불변 헌법 (Hard Rules)

본 문서는 Lumos 트레이딩 시스템을 유지보수하고 코드를 수정할 때 **어떠한 경우에도 임의로 누락하거나 훼손해서는 안 되는 절대 규칙(Hard Rules)**을 정의합니다. 모든 AI 에이전트와 개발자는 코드를 수정할 때마다 본 규칙을 최우선으로 준수해야 합니다.

---

## ⛔ [절대 금지 사항: Never Break]

1. **임의의 핵심 인터락 누락 금지 (No Side-Effect Regression)**:
   - 사용자가 특정 기능 수정을 요청했을 때, **하이브리드 MoE (GBDT 임계값), 크로스에셋 Veto, QQQ 60분봉 대추세 (피처 및 방패), ATR 트레일링 스탑, 양방향 매매(TQQQ/SQQQ)** 등 연관된 핵심 매매 로직을 절대로 건드리거나 생략하지 않는다.
   - **(단, 크로스에셋 Veto 방패 및 QQQ 60분봉 추세 필터(방패 2종)는 사용자의 의도에 따라 `config.py`의 `USE_CROSS_ASSET_VETO`, `USE_60M_TREND_FILTER` 값(True/False)으로 제어 및 무력화될 수 있도록 예외적으로 허용하며, 코드는 반드시 이를 존중해야 한다.)**
2. **사전 영향도 분석 없는 즉흥 수정 금지**:
   - 코드를 수정하기 전, 변경 대상 함수의 호출 관계와 사이드 이펙트를 완벽히 파악한다.
3. **설정값(Config) 하드코딩 절대 금지**:
   - GBDT 임계값(Threshold) 등 설정값은 오직 `config.py`에만 존재하는 변수이다. 모든 `.py` 파일은 임계값 등의 설정 숫자를 직접 하드코딩해서는 안 되며, 반드시 `config.py`에서 변수로 임포트하여(가져다) 써야 한다. (단, 백테스트 목적의 샌드박스나 새로 생성한 임시 스크립트 파일 내에서 테스트 용도로만 변수를 선언하여 사용하는 것은 예외로 허용한다.)
4. **가짜 수익률(Illusion) 창출 금지**:
   - 시뮬레이션 및 백테스트 진행 시 **왕복 거래 수수료 및 슬리피지는 반드시 최소 0.20% (0.0020) 이상**으로 설정하여 가짜 수익률이 계산되지 않도록 엄격히 통제한다.
5. **정합성 점검 시 config.py 임의 수정 금지**:
   - 정합성 확인 작업 중 룰 위반을 발견하더라도, 사용자가 명시적으로 "config.py를 수정하라"고 지시하기 전까지는 `config.py`를 절대 임의로 변경해서는 안 된다. 문제 해결이 필요하다면 `config.py`가 아닌 관련된 `.py` 파일의 로직을 수정하여 대응해야 한다.
6. **무결성 회귀 테스트 미실행 금지**:
   - 코드 수정 후에는 반드시 `python tests/test_system_integrity.py`를 실행하여 핵심 인터락이 정상 동작함을 검증한다.
7. **백테스트 시 미래참조(Look-ahead bias) 원천 차단 및 Config 절대 준수**:
   - 백테스트 스크립트 작성 시 어떠한 형태의 **미래 참조**(예: 현재 캔들의 고가를 먼저 반영하고 저가를 평가)도 있어서는 안 된다. 
   - 매수/매도/청산 기준은 오직 `config.py`의 룰을 참조하여 진행해야 하며, 어떠한 상황에서도 에이전트가 `config.py`를 임의로 수정해서는 안 되고 그 안의 설정값을 절대적으로 따라야 한다.
   - **5분봉 궤적으로 매도(청산)를 체크할 때, 단일 캔들 내에서 익절(TP)과 손절(SL) 라인을 동시에 터치할 경우 무조건 손절(SL)이 우선 체결된 것으로 가장 비관적/보수적으로 동작하도록 구현해야 한다.**
---

## 🔒 [핵심 6대 매매/운영 불변 룰 (Core Trading Rules)]

### 1. 🎯 진입 3대 인터락 및 하이브리드 MoE 의사결정 절대 기준
- **GBDT 파형 모델 단일 트리거 (임계값 ≥ 60%)**: 오직 15분봉 GBDT 방향 및 확신도(≥60%)가 최우선 조건이다.
- **크로스에셋 Veto 및 GBDT 인자 통합**: 크로스에셋(NVDA, QQQ 등) 데이터는 GBDT 모델의 학습 인자(Feature)로 융합되어 판단의 정확도를 높일 뿐만 아니라, GBDT 신호 방향과 크로스에셋 역풍이 감지될 경우 진입을 엄격히 차단하는 **독립적인 Veto 방패**로도 이중 작동한다.
- **다중 스크린 필터**: QQQ 60분봉 추세(EMA 기준) 필터를 통과해야 한다. (RSI 과매수/과매도 족쇄는 폐지됨)
- *(단, 기존에 존재했던 일일 3-Out 서킷브레이커는 악성 억제기로 판명되어 영구히 폐지되었다.)*
- **학습 데이터 롤링 원칙**: 매주 직전 2년(104주) 데이터를 기반으로 모델을 학습시켜 최신 상태를 유지한다 (백테스트와 실전 매매 동일).

### 2. 🏛 100% 증권사 원장 통신 동기화 (Single Source of Truth)
- 메모리 가상 변수(`_active_position`)로 매도/청산을 판단하지 않는다.
- **오직 키움 증권사 원장에 실제 체결 입고된 주식(poss_qty > 0)이 있을 때만** 매도 청산 로직을 집행한다.
- 매수 미체결 상태에서는 어떠한 청산 시도도 하지 않는다.

### 3. 🛡️ 실전 청산 원칙 (ATR 트레일링 스탑) 및 장 마감 청산
- 5분봉 기준 **ATR 트레일링 스탑**을 사용하여 추세를 극대화한다.
- **목표 익절 (Max TP)**: +10.0%
- **손절 라인 (ATR SL)**: max(2.0%, min(3.2%, ATR(14) * 1.5))
- **트레일링 발동 (Trailing Trigger)**: 매수가 대비 +1.5% 수익 도달 시 발동. 이후 고점 대비 -0.3% 하락 시 즉각 청산하여 추격 익절.
- **장 마감 10분 전(15:50 NYT, 04:50 KST)**에만 0% 오버나잇 전량 시장가 청산을 단독 수행한다. (CarryOverClear 금지)

### 4. ⏳ 미체결 스마트 타임아웃 & 단일 포지션 릴레이 원칙
- **단일 포지션 릴레이 (중복 진입 차단)**: 포지션을 보유 중이거나 미체결 상태일 경우, 신규 GBDT 매수 신호가 발생해도 즉각 무시(Jump)하여 수수료 출혈을 막는다.
- **매수 미체결(3초 타임아웃)**: 3초간 미체결 시 자동 취소 ➔ 최신 호가(Ask +0.03$)로 1회 재발주 ➔ 지속 미체결 시 완전 취소하여 예수금 100% 보존 후 즉시 다음 매매 신호 대기.
- **매도 미체결(3초 타임아웃)**: 3초간 미체결 시 자동 취소 ➔ 원장 잔고 재확인 후 최신 호가(Bid -0.05$)로 가격을 낮춰 반드시 100% 전량 청산 완결.

### 5. ⏱️ 투자 진행 필수 체크리스트: 글로벌 시간 동기화
- **투자 가동 전 체크리스트 1번 검증**: 로컬 OS 시계와 뉴욕 거래소 시계(`ZoneInfo("America/New_York")`) 시차 일치 검증 통과 필수.
- **모델 스위칭 상호 배타성 보장**:
  - Main Phase (15m GBDT):  9:30 ~ 15:30 EDT
  - 쿨다운 (신규 진입 차단): 15:30 ~ 15:50 EDT
  - EOD 100% 현금화 청산: 15:50 ~ 16:00 EDT

### 6. 💰 진입 비중 및 슬리피지 방어 (98% Allocation Rule)
- **증거금 부족 에러(Insufficient Funds) 원천 차단**: 매수 주문 시 호가를 올려서(Ask + .03) 시장가처럼 추격 매수를 진행하므로, 가용 예수금(usd_avail)의 100%를 꽉 채워 수량을 계산하면 키움증권 서버에서 증거금 부족으로 주문이 거부될 수 있습니다.
- **적정 비중 98% 고정**: 따라서 진입 수량(`order_qty`)을 계산할 때는 반드시 **가용 USD의 98% (`usd_avail * 0.98`)** 만을 사용하여 2%의 넉넉한 현금 버퍼를 남겨야 합니다.
- **수량 계산 공식**: `order_qty = int((usd_avail * 0.98) / (cur_px + 0.05))`
  *(현재가보다 .05 비싸게 산다고 가정하여 분모를 키움으로써 안전 버퍼를 이중으로 확보)*
- **어떤 에이전트라도 이 98% 룰을 100%나 90% 등으로 임의 변경해서는 절대 안 됩니다.**

### 7. 🧪 매매 테스트 (Mock Trading Test) 원칙
- **목적**: 시스템 업데이트 및 코드 수정 후 실전 매매에서 발생할 수 있는 에러 및 예외를 사전에 방지하기 위함.
- **테스트 기간**: 매매 테스트 시뮬레이션 기간은 너무 길어지지 않도록 최신 **1주일** 단위로 진행하는 것을 원칙으로 한다.
- **방식**: 키움증권 소켓 스트리밍 환경을 동일하게 모사(Mocking)하여, 데이터베이스에 저장된 최신 과거 지수 데이터(TQQQ 등 모든 연관 종목)를 기반으로 모델 판단 로직(GBDT 값, 롱숏관망 판단 등)을 100% 시뮬레이션한다.
- **사용 파일**: `scripts/live_trade_test_clean.py` 및 `core/mock_kiwoom_feeder.py` 를 실행하여 검증한다 (기존 실전 파일 절대 수정 금지).
- **데이터 보존**: 시뮬레이션 결과 및 AI 판단 로그는 반드시 백테스트처럼 파일(`data/trade_test_logs.txt`)에 누적 저장하여 시그널 이상 유무를 검토해야 한다.
- **에러 노출 (No Try-Catch)**: `scripts/live_trade_test_clean.py` 및 `core/mock_kiwoom_feeder.py` 내에서는 에러를 덮어두고(silently pass) 진행하는 `try-except` 구문을 절대 사용하지 않는다. 에러 발생 시 즉각 스크립트가 크래시(Crash)되도록 두어 버그를 즉각적으로 인지할 수 있도록 한다.

### 8. 📊 백테스트 (Backtest) 원칙
- **목적**: 모델이나 파라미터를 변경하면서 전략의 수익성과 안정성을 검증하는 실험실 역할.
- **테스트 기간**: 주 단위 롤링을 원칙으로 하되, 월 단위 백테스트 시에는 정확히 월~금 주기가 맞도록 전월/차월로 날짜가 살짝 넘어가도 무방하다.
- **데이터 롤링 학습**: 대상 주차의 백테스트를 수행할 때는, 반드시 해당 주차 기준 직전 과거 2년치 데이터를 롤링(Walk-Forward)하여 모델을 재학습해야 한다.
- **다중 해상도 캔들 검사 (15m / 5m)**: 학습을 15분봉으로 진행했으므로, 매수 진입 시그널 검사는 15분봉(15m) 기준으로 수행하고, 진입 후 매도(청산) 타점 및 궤적 관찰은 반드시 **5분봉(5m)** 기준으로 정밀하게 수행해야 한다.
- **초고속 백테스트 (Vectorized)**: 방대한 5분봉 데이터의 시뮬레이션 속도 저하를 막기 위해 무분별한 for loop(행 단위 반복)을 지양하고, 가급적 Pandas/Numpy의 벡터화(Vectorization) 연산을 활용하여 초고속으로 백테스트가 돌 수 있도록 설계해야 한다.
- **사용 파일 보존 (복사본 사용)**: 기존 백테스트 코드 원본을 직접 수정하여 훼손하지 말고, 반드시 원본을 복사하여 임시 샌드박스 파일(예: `temp_run_...py`)을 생성한 뒤 수정해서 테스트를 진행해야 한다.
- **결과 표 정리 의무**: 백테스트 완료 후 결과는 반드시 마크다운 표(Table) 포맷으로 보고해야 한다. 소제목으로 '테스트 기간'을 명시하고, 표의 항목은 **[종목, 총 수익률, 주별 매매횟수, 연평균수익률(CAGR), MDD]** 필드를 반드시 포함해야 한다.
- **시각화 의무 (Visual Report)**: 백테스트를 수행한 후에는 반드시 `scripts/plot_combined_all.py` 등 시각화 스크립트를 실행해 차트를 생성하고, 그 이미지를 대화창에 직접 렌더링(Embed)하여 사용자에게 즉시 보여주어야 한다.
- **미래 참조(Look-ahead bias) 원천 차단 및 보수적 청산**: 단일 캔들(15분/5분) 내에서 청산 시뮬레이션을 수행할 때, 절대 현재 캔들의 고가(High)로 트레일링 스탑을 먼저 갱신한 뒤 저가(Low)를 평가하는 오류를 범해서는 안 된다. 트레일링 스탑 기준선은 무조건 **이전 캔들까지의 최고점**으로 한정해야 한다. 또한 하나의 캔들 안에서 익절(TP)과 손절(SL) 라인을 모두 터치할 만큼 변동성이 컸다면, 거리 계산 등의 꼼수 없이 무조건 **손절(SL)**이 먼저 체결된 것으로 가장 비관적이고 보수적으로 처리하여 가짜 수익(Illusion)을 원천 차단해야 한다.
---

## 🛠️ [코드 수정 시 필수 워크플로우]

1. **스코프 한정**: 사용자가 요청한 범위 외의 핵심 로직은 절대 건드리지 않는다.
2. **코드 수정 전후 Diff 자체 점검**: 핵심 조건문(`final_conf >= 0.62`, `holdings > 0`, `_manage_unfilled_orders`)이 보존되었는지 확인.
3. **회귀 테스트 필수 실행**: `python tests/test_system_integrity.py` 통과 필수.
